import { BgResultadoDirective } from './bg-resultado.directive';

describe('BgResultadoDirective', () => {
  it('should create an instance', () => {
    const directive = new BgResultadoDirective();
    expect(directive).toBeTruthy();
  });
});
