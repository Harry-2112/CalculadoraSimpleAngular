import { BgHeaderDirective } from './bg-header.directive';

describe('BgHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new BgHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
